# Temporal Calibration Pilot — pinned dependencies
#
# NOTE ON TORCH / KAGGLE:
# Kaggle and Colab GPU images ship a CUDA-matched build of torch pre-installed.
# Do NOT `pip install torch` from this file on those platforms — it can replace
# the CUDA build with a CPU build and break GPU inference. The notebook installs
# only the packages below and leaves the platform torch untouched.
#
# For a local CUDA box, install a matching torch separately, e.g.:
#   pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121

transformers==4.48.3
accelerate==1.3.0
tokenizers==0.21.0
safetensors==0.4.5

# 4-bit (nf4) quantization so the 14B/32B R1 distills fit a 16 GB GPU.
# Must match the platform's torch CUDA build. Kaggle's torch is bleeding-edge
# (e.g. 2.10+cu128), for which an old pin fails transformers'
# is_bitsandbytes_available() check -- so the notebook force-upgrades to the
# newest build. Floor only here; do not hard-pin for Kaggle.
bitsandbytes>=0.46.1

numpy==1.26.4
pandas==2.2.2
matplotlib==3.8.4
seaborn==0.13.2
tqdm==4.66.5
rapidfuzz==3.10.1
python-dotenv==1.0.1

# torch is intentionally NOT pinned here — see note above.
# Tested against torch 2.4.x / 2.5.x with CUDA 12.x.
